#!/bin/bash

# https://www.terraform.io/docs/cli/index.html

alias tf='terraform'
alias tfi='terraform init'
alias tfv='terraform validate'
alias tfp='terraform plan'
# do not add an alias for `terraform apply` since we should be mindful with this
